/* THE DOOR — /reset.

   Setting a new password is a coming-back-in moment, not a first-visit
   one, so it wears the warm door for the same reason /login does when
   you're signing in: the person on this screen is already locked out and
   already frustrated. See app/door.css. */
import '../door.css';

export default function ResetLayout({ children }) {
  return children;
}
