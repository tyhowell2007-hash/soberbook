/* Same import list as the wall and the profile — this is a room, not a
   door, so it wears the green. See the note at the top of layout.jsx. */
import '../theme-green.css';
import '../photos.css';
export default function FindLayout({ children }) { return children; }
